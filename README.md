# Homebrew Tap for OpenAgent

```bash
brew tap ask-sol/openagent
brew install openagent
```

Then run `openagent` from any terminal.

## About

[OpenAgent](https://github.com/ask-sol/openagent) — Open-source agentic coding CLI. Multi-provider, token-efficient, extensible.
